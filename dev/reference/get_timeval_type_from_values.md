# Get time scale code from values (see TIMEVAL in px-specification)

Get time scale code from values (see TIMEVAL in px-specification)

## Usage

``` r
get_timeval_type_from_values(values)
```

## Arguments

- values:

  Values form PX-file

## Value

A character vector
