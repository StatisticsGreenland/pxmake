# Get variable code and label

Get a data frame with variable codes and label in all languages. If
VARIABLECODE is used, the relation is defined there, but otherwise the
variable code is set to be the main languages' label.

## Usage

``` r
get_variable_label(metadata_df)
```

## Arguments

- metadata_df:

  Data frame with metadata.

## Value

A data frame
