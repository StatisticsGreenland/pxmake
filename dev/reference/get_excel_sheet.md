# Get specific sheet from Excel workbook

Get specific sheet from Excel workbook

## Usage

``` r
get_excel_sheet(sheet, add_automatically = FALSE)
```

## Arguments

- sheet:

  String. Sheet to read.

- add_automatically:

  Logical. If TRUE, return an empty data frame if the sheet does not
  exist.

## Value

A data frame.
