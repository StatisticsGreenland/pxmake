# Greenlanders

A fictive data set with demographic data for Greenlanders split in two
cohorts.

## Usage

``` r
greenlanders
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 100
rows and 4 columns.
